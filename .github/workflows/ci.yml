name: ci
on: [push]

jobs:
    build:
        strategy:
            matrix:
                os: [linux-latest, macos-latest, windows-latest]
        runs-on: ${{matrix.os}}
        steps:
            - run: cmake